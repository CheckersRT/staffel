# `@staffel/workflow`

The deterministic core of Staffel. The package currently exposes configuration
validation, workflow policy, normalization, and versioned blank templates.

The future `@staffel/cli` package will provide the `staffel` command. It and all
filesystem, Git, tracker, and Codex integrations are intentionally outside this
extraction slice.
